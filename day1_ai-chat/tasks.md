# Execution Loop — Task Pool

## Documentation

- [x] **T01** (docs/S): Add JSDoc to `lib/rag/tool.ts` — Done: JSDoc present on `createRagTool` export ✅ PASS
- [x] **T02** (docs/S): Add JSDoc to `lib/memory.ts` — Done: JSDoc present on all exported functions ✅ PASS
- [x] **T03** (docs/S): Add JSDoc to `lib/task-state.ts` — Done: JSDoc present on all exports ✅ PASS

## Bug Fixes

- [x] **T04** (bug/S): Fix silent catch swallowing invariants in `app/page.tsx:122` — empty `catch {}` discards parse errors. Done: catch logs warning and resets to `[]` ✅ PASS
- [x] **T05** (bug/S): Fix `res.body!` non-null assertion in `app/page.tsx:277` — add null check before `.getReader()`. Done: throws descriptive error if `res.body` is null ✅ PASS
- [x] **T06** (bug/M): Fix silent catch blocks in `app/page.tsx` — lines 295, 514, 519, 572, 577 have empty `catch {}` or `catch { /* skip */ }`. Done: each catch logs a warning with context ✅ PASS
- [x] **T07** (bug/M): Add `ragThreshold` validation in `app/api/chat/route.ts` — reject NaN, negative, or >1 values. Done: invalid values return 400 with descriptive error ✅ PASS
- [x] **T08** (bug/M): Surface tool execution errors to UI in `app/page.tsx:597-601` — `handleToolAllow` catch only logs console.error. Done: error state set so error banner displays ✅ PASS

## Tests

- [x] **T09** (test/S): Write tests for `lib/sessions.ts` — cover getOrCreateAgent, session reuse, edge cases. Done: `lib/__tests__/sessions.test.ts` exists, vitest passes ✅ PASS
- [x] **T10** (test/M): Write tests for `lib/rag/tool.ts` — cover createRagTool with mocked search, error handling. Done: `lib/__tests__/rag/tool.test.ts` exists, vitest passes ✅ PASS
- [x] **T11** (test/M): Write tests for `lib/memory.ts` — cover extractMemory, error paths. Done: `lib/__tests__/memory.test.ts` exists, vitest passes ✅ PASS
- [x] **T12** (test/M): Write rendering test for `ErrorMessage` component — no tests exist. Done: `app/components/__tests__/ErrorMessage.test.tsx` exists and passes ✅ PASS

## Small Features

- [x] **T13** (feature/M): Add file upload size limit (50MB) in `app/page.tsx` — reject oversized files with user-visible error. Done: files over 50MB show error, not uploaded ✅ PASS
- [x] **T14** (feature/M): Graceful RAG fallback on indexing timeout in `lib/agent.ts:252-258` — disable RAG for session if indexing times out. Done: chat works when Ollama is down ✅ PASS
- [x] **T15** (feature/L): Clean up ObjectURL memory leak in `app/page.tsx:249-262` — revoke URLs on component unmount. Done: cleanup function present in useEffect ✅ PASS

## Refactoring

- [x] **T16** (refactor/M): Extract metrics/timing logic from `lib/agent.ts` into `lib/agent-metrics.ts`. Done: new file with named exports, agent.ts imports from it, build passes ✅ PASS
- [x] **T17** (refactor/L): Add session TTL to `lib/sessions.ts` — expire sessions after 24h with periodic cleanup. Done: stale sessions removed, existing tests still pass ✅ PASS
- [x] **T18** (refactor/L): Extract RAG setup logic from `lib/agent.ts` into `lib/agent-rag.ts`. Done: new file, agent.ts imports from it, build passes ✅ PASS

## Large Features

- [x] **T19** (feature/L): Conversation export — add `app/api/chat/export/route.ts` that accepts a `sessionId` query param, reads messages+files from `lib/db.ts` via `getSessionMessagesWithFiles()`, and returns a downloadable markdown file with all messages formatted as `### User` / `### Assistant` blocks with timestamps. Add an "Export" button to `app/components/ChatContainer.tsx` (or the appropriate header area) that calls this endpoint and triggers a browser download. Done: clicking Export downloads a `.md` file containing the full conversation; the API route returns `Content-Disposition: attachment` header; build passes ✅ PASS
- [x] **T20** (feature/L): Session history sidebar — add `app/api/chat/sessions/route.ts` GET endpoint that queries `SELECT DISTINCT session_id, MIN(created_at) as started, MAX(created_at) as last_active, COUNT(*) as message_count FROM messages GROUP BY session_id ORDER BY last_active DESC`. Create `app/components/SessionHistory.tsx` that renders a scrollable list of past sessions with date and message count. Clicking a session calls `app/api/chat/sessions/[sessionId]/route.ts` GET which returns messages via `getSessionMessages()`. Wire into `app/page.tsx` — load selected session's messages into state. Done: sidebar shows past sessions; clicking one loads its messages; current session appears at top; new `SessionHistory` component has a paired test file; build passes ✅ PASS
- [x] **T21** (feature/L): Keyboard shortcuts system — create `lib/keyboard-shortcuts.ts` exporting a `registerShortcuts(handlers)` function that listens for: `Cmd/Ctrl+K` (new chat — calls existing `handleNewChat`), `Cmd/Ctrl+/` (toggle sidebar), `Cmd/Ctrl+Shift+C` (copy last assistant message), `Escape` (close any open dialog). Create `app/components/KeyboardShortcuts.tsx` as a `'use client'` component that uses `useEffect` to register/cleanup listeners on mount/unmount, reading handlers from props. Create `app/components/ShortcutsDialog.tsx` that shows a help overlay listing all shortcuts (triggered by `?` when not focused on input). Wire both into `app/page.tsx`. Done: all 4 shortcuts work; `ShortcutsDialog` renders on `?` keypress; both components have paired test files; build passes ✅ PASS
- [x] **T22** (feature/L): Message reactions & bookmarks — add a `bookmarks` table to `lib/db.ts` schema: `CREATE TABLE IF NOT EXISTS bookmarks (id INTEGER PRIMARY KEY, session_id TEXT NOT NULL, message_index INTEGER NOT NULL, label TEXT DEFAULT 'bookmark', created_at TEXT DEFAULT (datetime('now')))` with a unique index on `(session_id, message_index)`. Add `saveBookmark(sessionId, messageIndex, label)`, `removeBookmark(sessionId, messageIndex)`, `getBookmarks(sessionId)` exports. Create `app/api/bookmarks/route.ts` with GET (list by session), POST (add), DELETE (remove) handlers. Create `app/components/BookmarkButton.tsx` — a small star/bookmark icon that toggles on click, calling the API. Add it to each message row in the chat UI. Create `app/components/BookmarksList.tsx` — a panel listing all bookmarked messages for the current session with click-to-scroll. Done: bookmarking a message persists to DB; bookmark icon toggles visually; bookmarks list shows all saved messages; both new components have paired test files; build passes ✅ PASS
- [x] **T23** (feature/L): Conversation search — add FTS5 full-text search to `lib/db.ts`: create a virtual table `CREATE VIRTUAL TABLE IF NOT EXISTS messages_fts USING fts5(content, session_id UNINDEXED)` and add triggers to keep it in sync with the `messages` table (INSERT/DELETE). Add `searchMessages(query: string, sessionId?: string): Array<{session_id, message_index, role, content, snippet}>` export that uses `messages_fts MATCH` with `snippet()` for highlighted excerpts. Create `app/api/chat/search/route.ts` GET endpoint accepting `q` and optional `sessionId` query params, returns JSON array of results. Create `app/components/SearchMessages.tsx` — a `'use client'` component with a search input (appears via `Cmd/Ctrl+F`), debounced query (300ms), results list showing message snippets with session context, and click-to-scroll to the matching message in the current session (or load the session first if searching across sessions). Wire into `app/page.tsx` with a `useEffect` listener for the keyboard shortcut. Done: `Cmd/Ctrl+F` opens search overlay; typing shows live results with highlighted matches; clicking a result scrolls to or loads that message; FTS table stays in sync; `SearchMessages` has a paired test file; build passes
- [x] **T24** (feature/L): System prompt templates — create `lib/system-prompts.ts` with a `SystemPrompt` interface `{ id: string; name: string; content: string; isDefault: boolean; createdAt: string }` and functions: `getSystemPrompts(): SystemPrompt[]`, `getDefaultSystemPrompt(): SystemPrompt | null`, `saveSystemPrompt(name, content, isDefault?): string`, `deleteSystemPrompt(id): void`, `setDefaultSystemPrompt(id): void`. Back with a new `system_prompts` table in `lib/db.ts`: `CREATE TABLE IF NOT EXISTS system_prompts (id TEXT PRIMARY KEY, name TEXT NOT NULL, content TEXT NOT NULL, is_default INTEGER DEFAULT 0, created_at TEXT DEFAULT (datetime('now')))`. Seed with one default prompt: "You are a helpful AI assistant." Create `app/api/system-prompts/route.ts` with GET (list all), POST (create), DELETE (remove by id) handlers, and `app/api/system-prompts/[id]/default/route.ts` PUT to set default. Create `app/components/SystemPromptSelector.tsx` — a `'use client'` dropdown in the chat header area showing available prompts, with a "Manage" button that opens `app/components/SystemPromptDialog.tsx` for CRUD (create/edit/delete prompts with a textarea and name input). The selected prompt should be passed to the chat API via a `systemPrompt` field in the request body. Modify `app/api/chat/route.ts` to accept and use `systemPrompt` — prepend it as a system message before user messages when calling the agent. Done: dropdown shows saved prompts; selecting one changes the active system prompt; Manage dialog allows create/edit/delete; selected prompt is sent with chat requests and used by the agent; all new components have paired test files; build passes
